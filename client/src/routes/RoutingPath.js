const homeView = "/"
const signInView = "/signin"
const profileView = "/profile"
const settingsView = "/settings"
const recipeView = "/recipe"

export default {
    homeView,
    signInView,
    profileView,
    settingsView,
    recipeView
}